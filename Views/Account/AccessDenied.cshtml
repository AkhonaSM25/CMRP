@{
    ViewData["Title"] = "Access denied";
}

<div class="card-app empty-state">
    <span class="empty-icon"><svg class="icon"><use href="#i-lock" /></svg></span>
    <h1 class="h4">You don't have access to this page</h1>
    <p class="muted">Your account does not have permission to view this. If you think that is a mistake, ask a CMRP administrator.</p>
    <a class="btn btn-primary" asp-controller="Home" asp-action="Index">Go to home</a>
</div>
